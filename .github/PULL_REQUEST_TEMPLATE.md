## Summary

What does this change do and why?

## Checklist

- [ ] `zig fmt --check src examples tests build.zig` passes
- [ ] `zig build test` passes (no leaks)
- [ ] `zig build test-integration` passes (when applicable)
- [ ] CHANGELOG.md updated under `[Unreleased]` (when user-visible)
- [ ] Docs updated (README / AGENTS.md / docs/) when behavior changed

## Testing

How was this verified? List the commands and any manual checks.
